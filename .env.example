# En dev local, laisser vide : le frontend appelle /api/* directement
# (l'API doit tourner sur http://localhost:8787)
VITE_API_URL=

# En production, pointer vers l'URL de l'API déployée
# VITE_API_URL=https://lucgobet-lartisandubois-api.workers.dev
